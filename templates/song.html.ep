% my $nav = begin
  <div class="columns">
    <div class="column col-6">
    % if (defined $previous) {
      <a class="btn" href="<%= $previous->{slug} %>">« <%= $previous->{name} %></a>
    % }
    </div><div class="column col-6 text-right">
    % if (defined $next) {
      <a class="btn" href="<%= $next->{slug} %>"><%= $next->{name} %> »</a>
    % }
    </div>
  </div>
% end
%= $nav->()
%== $content
%= $nav->()
